Deployments
ans - deployments manages set of pods to manage application workload,one that not maintain state.
the replicaset is kubernetes controller.

key points about deployment
there are deployment controllers in kubernetes which are responsible for maintaining the desired state and actual state in kubernetes cluster.

what is the command to list out all the resources and namespaces inside the cluster.

cmd  - kubectl get all -A .

controller function - the controller is function written in go language used to perform specific tasks 
exm - replicaset controller will ensures that actual pods and desired number of pods are there inside the cluster.

1)command to create a deployment inside kubernetes cluster.
kubectl apply -f deployment.yaml

2)command to delete kubernetes deployment
kubectl delete deployment deployment.yaml

3)
